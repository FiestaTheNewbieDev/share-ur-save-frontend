/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'media.rawg.io',
			'placehold.co',
			'firebasestorage.googleapis.com',
		],
	},
};

export default nextConfig;
